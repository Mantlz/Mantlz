import { billingMetadata } from '@/config/metadata'

export const metadata = billingMetadata

export default function BillingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
} 