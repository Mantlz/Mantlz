import { campaignMetadata } from '@/config/metadata'

export const metadata = campaignMetadata

export default function CampaignsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
} 